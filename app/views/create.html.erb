<%= form_with(url: searches_path, method: :post, remote: true) do |form| %>
  <%= form.text_field :query, placeholder: "Search...", autofocus: true %>
  <%= form.submit "Search" %>
<% end %>

<%= render partial: 'search_results' %>